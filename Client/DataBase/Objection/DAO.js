const objection = require('objection');
const { Model, ref } = require('objection');
const { initialize } = require('objection');
await initialize([Project])
(async () => {
    const person = await Objection.Model;
    console.log(person)

})()
